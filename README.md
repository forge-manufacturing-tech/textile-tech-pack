# Interlock Frontend

This is the frontend for the Interlock SaaS application. It is a Vite application intended to run against the local backend. 

`VITE_API_URL` determines the base URL for API requests. Set it to `http://localhost:5150/api` to set the local backend.